/*
    * Design a function called yell that consumes strings like "hello" and adds
    * "!" to produce strings like "hello!"
*/

// Signature
// * String: String

// Purpose
// * Returns the given string appended with "!" at the end

// Stub
// * export default function yell(word: string): string { return "string" };

// Tests -> See yell.test.ts

// Template
/*
* export default function yell(word: string): string { 
      * DO SOMETHING WITH STRING
* };
*/

export default function yell(word: string): string {
    return word + '!'
};